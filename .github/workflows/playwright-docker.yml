name: Playwright Tests Docker
on: 
  workflow_dispatch:
    inputs:
      testOptions:
        description: 'Test options'
        default: ''
        requires: false
        type: string

jobs:
  myTest:
    name: Running tests
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: Hocker/setup-docker-action@v4
    - name: Building Docker Image 
      run: docker build -t playwright-test-image .
    - name: Running Docker
      run: docker run playwright-test-image